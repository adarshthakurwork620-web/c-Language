#include<stdio.h>
int main()
{
    int a;
    printf("enter value of a:");
    scanf("%d",&a);
    if (a>0)
    
        printf("A is positive:");
    
    if (a<0)
    
        printf("A is negitive:");
    
    if(a==0)
    
        printf("A is zero:");
    
    return 0;
    
}